[package]
name = "apollo-proto"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared IPC types and wire protocol between apollod and apolloctl"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
